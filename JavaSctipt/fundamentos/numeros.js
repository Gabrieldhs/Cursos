const peso1 = 1.0
const peso2 = Number('2.0')

console.log(peso1,peso2)
console.log(Number.isInteger(peso1)) // retorna bool
console.log(Number.isInteger(peso2)) // retorna bool

const nota1 = 9.0435
const nota2 = 6.9594

const total = nota1 * pesoa1 + nota2 * peso2
const media = total / (peso1 + peso2)

console.log(media.toFixed(2))// retornar 2 casas decimais
console.log(media.toString(2))// em binário
console.log(typeof media)
console.log(typeof Number)